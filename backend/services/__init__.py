from .socket_service import initialize_socketio

    # If we add more services in the future, we can import them here
    # For example:
    # from .ai_service import some_ai_function

    # This allows us to do `from services import initialize_socketio` in other parts of our app